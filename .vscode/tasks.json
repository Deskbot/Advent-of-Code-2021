{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "zig build",
            "type": "shell",
            "command": "zig build"
        },
        {
            "label": "v build",
            "type": "shell",
            "command": "./v/v",
            "args": ["src/main.v", "-o", "v-out/main"]
        }
    ]
}
